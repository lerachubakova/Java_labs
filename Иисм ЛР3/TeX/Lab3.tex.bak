\addtocounter{chapter}{1}
%\section*{Лабораторная работа 3}
\section*{Условие:}
Смоделировать непрерывную случайную величину. Исследовать точность моделирования.
\begin{enumerate}
\item Осуществить моделирование $n=1000$ реализаций СВ из нормального закона распределения $N(m,s^2)$ с заданными параметрами. Вычислить несмещённые оценки математического ожидания и дисперсии, сравнить их с истинными.
\item Смоделировать $n=1000$ СВ из заданных абсолютно непрерывных распределений. Вычислить несмещённые оценки математического ожидания и дисперсии, сравнить их с истинными значениями(если это возможно).
\item Для каждой из случайных величин построить свой критерий Колмогорова с уровнем значимости $\varepsilon=0.05$. Проверить, что вероятность ошибки I рода стремится к $0.05$.
\item Для каждой из случайных величин построить свой $\chi^2$--\,критерий Пирсона с уровнем значимости $\varepsilon = 0.05$. Проверить, что вероятность ошибки I рода стремится к $0.05$.
\item Осуществить проверку каждой из сгенерированных выборок каждым из построенных критериев.
\end{enumerate}
\newpage
\section*{Теория:}
\subsubsection*{Нормальное распределение (0,64):}
НСВ $\xi\in \mathbb{R}$  с плотностью распределения\begin{equation}
p_\xi(x)=\frac1{\sqrt{2\pi}\sigma}\exp\left\lbrace - \frac{(x-\mu)^2}{2\sigma^2}\right\rbrace
\notag
\end{equation}
имеет одномерное нормальное распределение $N_1(\mu,\sigma^2)$ с параметрами: средним значением $\mu \in \mathbb{R}$ и дисперсией $\sigma^2>0$.

Математическое ожидание: $\mu$.

Дисперсия: $\sigma^2$.

Распределение $N_1(0,1)$ называется стандартным нормальным распределением, а НСВ $\eta\sim N_1(0,1)$ ---стандартной нормальной (гаусовской) величиной. Случайные величины $\xi$ и $\eta$ связаны соотношением: $\xi=\eta+\sigma\cdot\eta$, где $\xi$ имеет распределение $N_1(\mu,\sigma^2)$.

\subsubsection*{$\chi^2$--распределение(4):}
НСВ $\xi\in[0,+\infty)$ с плотностью распределения 
\begin{equation}
p_{\xi}(x)=\frac{x^\frac{(m-2)}{2}e^{-\frac{x}{2}}}{2^{m/2}\Gamma(m/2)},\quad x \geqslant0,\notag 
\end{equation}
имеет $\chi^2$--распределение $\chi^2(m)$ с $m$ степениями свободы $(m>0,\quad m\in \mathbb{N})$.Здесь $\Gamma(z)$ --- гамма-функция Эйлера.

Среднее значение: $\mu = m$.

Дисперсия: $\sigma^2=2m$.

Известно, что, если $\eta_1, \dots, \eta_m\sim N_1(0,1)$ --- независимые стандартные гаусовские СВ, то СВ $\xi = \sum_{i=1}^m\eta_i^2$ имеют $\chi^2$--распределение.

\subsubsection*{Распределение Фишера (5,3):}
НСВ $\xi\in[0,+\infty)$ с плотностью распределения
\[ p_\xi(x)=\frac{\Gamma\left(\frac{l+m}2\right)x^{\frac{l}2-1}\left(\frac{l}{m}\right)^{\frac{l}2}}{\Gamma\left(\frac{l}{2}\right)\Gamma\left(\frac{m}2\right)\left(1+\frac{l}{m}x\right)^{\frac{l+m}{2}}}, \quad x\geqslant0 \]
имеет распределение Фишера $F(l,m)$ c $l$ и $m$ числом степеней свободы. \\Здесь:
$l, m$ --- натуральные числа, параметры распределения;
$\Gamma(z)$ --- гамма-функция Эйлера.

Среднее значение: $\displaystyle\mu = \frac{m}{m-2},\quad m>2$.

\smallskip

Дисперсия: $\displaystyle\sigma^2=\frac{2m^2(l+m-2)}{l(m-2)^2(m-4)},\quad m>4$.

Пусть $\xi_l\sim\chi^2(l),\xi_m\sim\chi^2(m)$ --- НСВ, а СВ $\xi$ определяется соотношением \[\xi = \frac{\xi_l/l}{\xi_m/m},\] тогда СВ $\xi$ имеет распределение $F(l,m)$.
\subsection*{Результат выполнения:}
\lstinputlisting[numberstyle=\small\color{white}, stringstyle=\color{black}]{../java/result.txt}

\newpage

\subsection*{Код программы:}
\lstinputlisting[language=Java]{../java/src/SSM_Main3.java}
